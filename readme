Create a new repository on the command line

touch readme
git init
git add readme
git commit -m "first commit"
git remote add origin https://github.com/cedarli/pdf.git
git push -u origin master
Push an existing repository from the command line

git remote add origin https://github.com/cedarli/pdf.git
git push -u origin master
